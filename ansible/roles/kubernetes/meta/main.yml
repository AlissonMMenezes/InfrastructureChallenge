---
galaxy_info:
  role_name: kubernetes
  author: InfrastructureChallenge
  description: Kubeadm cluster node prep, control plane, workers, Calico CNI, verify (used by playbooks/bootstrap-k8s.yml in phases).
  license: Apache-2.0
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
    - name: Debian
  galaxy_tags:
    - kubernetes
    - kubeadm
    - containerd
    - calico

dependencies: []
