---
# Order: cluster identity checks → containerd → Kubernetes packages + kubelet.
- name: Run prechecks
  ansible.builtin.include_tasks: prechecks/main.yml

- name: Prepare Kubernetes nodes (container runtime + kube packages)
  ansible.builtin.include_tasks: node_prep/main.yml
