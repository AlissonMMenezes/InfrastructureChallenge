apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # Namespace must exist before HelmRelease (metadata.namespace: traefik-system).
  - namespace.yaml
  - helmrepository.yaml
  - helmrelease.yaml
