apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: app-db
  namespace: data-prod
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: app-db
  podMetricsEndpoints:
    - port: metrics
