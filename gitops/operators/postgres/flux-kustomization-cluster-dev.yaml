# Flux: reconcile CNPG Cluster + backups + monitors for dev (Git path → kustomize build)
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: postgres-cluster-dev
  namespace: flux-system
spec:
  interval: 10m
  path: ./gitops/operators/postgres/manifests/dev
  prune: true
  wait: true
  timeout: 5m
  sourceRef:
    kind: GitRepository
    name: flux-system
  dependsOn:
    - name: cnpg-operator
