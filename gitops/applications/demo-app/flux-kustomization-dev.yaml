# Flux: demo application (dev)
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: demo-app-dev
  namespace: flux-system
spec:
  interval: 10m
  path: ./gitops/applications/demo-app/manifests/dev
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
